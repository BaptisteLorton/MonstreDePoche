package MonstreDePoche.models.actions;

import MonstreDePoche.models.Player;

public class UseObjectAction  extends Action {
    
    public UseObjectAction(Player player, String message) {
        super(player, message);
    }
}
